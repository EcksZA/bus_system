<% content_for(:title, 'Edit | Line') %>

<h1>Edit Line</h1>

<%= render('layouts/errors', :object => @line) %>

<%= render('form') %>

<p><%= link_to "Delete", line_path(@line),
                      :data => { :confirm => "Are you sure you want to delete this line?",
                      :method => 'delete'},
                      :class => 'btn btn-danger' %>
</p>
